load("//bazel:flags.bzl", "CPP_FLAGS", "MAPLIBRE_FLAGS")

cc_binary(
    name = "cache_tool",
    srcs = [
        "cache.cpp",
    ],
    copts = CPP_FLAGS + MAPLIBRE_FLAGS,
    deps = [
        "//platform:macos-objcpp",
    ],
)
