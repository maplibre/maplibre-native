<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Both Dawn and wgpu-native use Vulkan on Android -->
    <uses-feature
        android:name="android.hardware.vulkan.version"
        android:version="0x400003"
        android:required="true" />
</manifest>
