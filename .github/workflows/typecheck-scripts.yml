name: typecheck-scripts

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  typecheck-scripts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - run: |
          npm install
          npx tsc -p tsconfig.json
